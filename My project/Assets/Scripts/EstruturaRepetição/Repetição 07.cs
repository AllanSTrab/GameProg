using UnityEngine;

public class Repetição07 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
        for (int x = 0; x <= 1000; x+= 5)
        {
            print(x);
        }

    }
}
