using UnityEngine;

public class VetorEx02 : MonoBehaviour
{
    // Exemplo Vetor definido pelo código
    [SerializeField] string[] jogos = new string[5]; // Apenas Tamanho
    [SerializeField] string[] cores = new string[] {"Azul", "Preto", "Vermelho"};
    // Tamanho é definido pelo numero de elementos após as chaves
    void Start()
    {
        
    }
}